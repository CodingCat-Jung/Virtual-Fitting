
services:
  demo-app:
    build: .
    volumes:
      - .dockervenv:/src/.venv
      - .:/src  # 현재 경로를 /src로 마운트
    ports:
      - "8000:8000"
    environment:
      - WATCHFILES_FORCE_POLLING=true

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: 'qkrwogus1205!'
      MYSQL_DATABASE: 'Virtual-Fitting'
      TZ: 'Asia/Seoul'
    volumes:
      - mysql_data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - 33306:3306
volumes:
  mysql_data: